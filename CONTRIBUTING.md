# 贡献指南

[English](CONTRIBUTING.en.md) · [首页](README.md)

## 开始前

使用 Node.js `>=22.13`，在仓库根目录执行 `npm ci`。先阅读[快速开始](docs/getting-started.md)、[架构](docs/architecture.md)和[Agent 接入](docs/agent-access.md)。保持修改聚焦，不覆盖其他贡献者的未提交改动。

## 变更流程

1. 说明目标、允许文件与验证方法。
2. 先理解现有合同、路由和数据边界，再做最小修改。
3. 对代码运行相关 `test:*`、`npm run lint` 或构建检查；对文档检查链接和示例。
4. 在变更说明中写明修改内容、已运行验证和未覆盖风险。

不要在仓库提交密钥、真实账号资料、Provider 端点、浏览器会话或未脱敏文章快照。不要将 Agent 输出直接提升为审批、合并或发布结果。

## 文档约定

面向陌生开发者写清前置条件、命令、边界和可复现步骤。中文与英文文档应语义同构并互相链接；使用相对路径和跨平台命令。事实不明时写为待运行时验证，不能把代码合同或静态配置写成外部连通、自动发布或公开可见。

## 提交前

确认新增相对链接存在、固定 origin 保持 `http://[::1]:3000`、平台仍被准确描述为 `manual` / `not_connected`，并且候选、审批、合并与发布没有被合并成单一权限。
